# Packaging (opcional)

Esta carpeta contiene archivos relacionados con empaquetado/instalador para Windows.

- `InventariosPOS.spec`
- `InstalarInventarios.spec`
- `installer.py`

Nota: el repositorio no versiona artefactos generados (por ejemplo `build/`, `dist/`, `dist_installer/`).
