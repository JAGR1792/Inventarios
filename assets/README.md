# Assets (icono)

Coloca aquí el icono de la app para Windows:

- `assets/app.ico`

Notas:
- Debe ser un archivo `.ico` (recomendado 256x256). 
- Si no existe, la app funciona igual (solo sin icono personalizado).
- El script de build detecta este archivo automáticamente para el `.exe`.
