# Local persistence (SQLite)
DATABASE_URL=sqlite:///instance/pos.sqlite

# Optional: import products from Excel (one-time or as needed)
EXCEL_IMPORT_PATH=GAROM OK.xlsx
EXCEL_WORKSHEET_NAME=Costos

# Excel engine: openpyxl (fast) or excel (Windows + Excel installed; best for formulas)
LOCAL_EXCEL_ENGINE=openpyxl

# Optional: folder for product images (desktop UI)
IMAGES_DIR=images
